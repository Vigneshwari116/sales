/// Shared local login — not sent to the server.
/// TODO: hash credentials instead of plain text if hardening is needed later.
const String appUsername = 'admin';
const String appPassword = 'admin123';
